# Firebase Configuration Template
# Copy this file to .env.local and fill in your actual Firebase credentials
#
# IMPORTANT: After updating .env.local, you MUST restart your dev server:
#   1. Stop the server (Ctrl+C)
#   2. Run: npm run dev
#
# To get your Firebase credentials:
# 1. Go to Firebase Console: https://console.firebase.google.com/
# 2. Select your project: salon-platform-34cec
# 3. Click gear icon → Project Settings → General tab
# 4. Scroll to "Your apps" section
# 5. Click Web app icon (</>) or select existing web app
# 6. Copy the configuration values below

# Firebase Web API Key (MUST start with "AIza" - this is NOT a server key or other type)
# Get from: Firebase Console → Project Settings → General → Your apps (Web)
# The API key should be ~40 characters and start with "AIza"
NEXT_PUBLIC_FIREBASE_API_KEY=AIza...

# Firebase Auth Domain (format: your-project-id.firebaseapp.com)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=salon-platform-34cec.firebaseapp.com

# Firebase Project ID
NEXT_PUBLIC_FIREBASE_PROJECT_ID=salon-platform-34cec

# Firebase Storage Bucket - use exact name from Firebase Console → Storage (e.g. your-project.appspot.com)
# Required for both client and server (logo upload). Use the SAME value for both vars.
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=salon-platform-34cec.appspot.com
FIREBASE_STORAGE_BUCKET=salon-platform-34cec.appspot.com

# Firebase Messaging Sender ID (numeric, from Firebase Console)
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=

# Firebase App ID (format: 1:123456789:web:abc123def456)
NEXT_PUBLIC_FIREBASE_APP_ID=

# Example of valid values (replace with your actual values):
# NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=salon-platform-34cec.firebaseapp.com
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=salon-platform-34cec
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=salon-platform-34cec.appspot.com
# FIREBASE_STORAGE_BUCKET=salon-platform-34cec.appspot.com
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
# NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890

# ============================================================================
# Firebase Admin SDK (Server-side only - for API routes)
# ============================================================================
# REQUIRED for production builds (Vercel)
# OPTIONAL for development (can use local JSON file instead)
#
# Option 1 (Recommended): Single JSON string
# Get from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# Copy the entire JSON content and paste it here as a single line (or use escaped newlines)
# IMPORTANT: In Vercel, paste the JSON as-is (it will handle newlines automatically)
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Option 2 (Alternative): Split into separate env vars
# FIREBASE_PROJECT_ID=salon-platform-34cec
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@salon-platform-34cec.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# Note: FIREBASE_PRIVATE_KEY should include the full key with \n for newlines

# ============================================================================
# Landing page CMS (admin editor at /admin/landing)
# ============================================================================
# Comma-separated list of emails allowed to edit the main landing page content.
# Example: NEXT_PUBLIC_LANDING_ADMIN_EMAILS=admin@example.com,you@example.com
NEXT_PUBLIC_LANDING_ADMIN_EMAILS=

# ============================================================================
# Twilio WhatsApp (booking confirmation + 24h reminder)
# ============================================================================
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
# Sender: sandbox "whatsapp:+14155238886" or your prod WhatsApp number
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
# Production (Vercel): exact URL Twilio calls — set in Vercel to fix signature validation behind proxy
# Must match Twilio Console "When a message comes in" (e.g. https://YOUR_APP.vercel.app/api/webhooks/twilio/whatsapp)
# TWILIO_WEBHOOK_URL=https://YOUR_APP.vercel.app/api/webhooks/twilio/whatsapp
# For signature validation when behind ngrok (local): https://abc123.ngrok.io
WEBHOOK_BASE_URL=
# Secure cron reminder job: Authorization: Bearer <CRON_SECRET>
CRON_SECRET=
# DEV only: set to "true" to skip Twilio webhook signature validation (never in production)
# SKIP_TWILIO_SIGNATURE=true

# ============================================================================
# Cloudinary (logo upload - signed upload, no preset)
# ============================================================================
# From Cloudinary Dashboard: Cloud name, API Key, API Secret
# API Secret must stay server-only (not NEXT_PUBLIC_).
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
