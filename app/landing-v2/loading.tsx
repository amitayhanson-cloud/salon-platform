import { LiquidGlassLoading } from "@/components/landing-v2/liquid-glass-loading"

/**
 * Shown while the /landing-v2 segment is loading (navigation or initial load).
 */
export default function LandingV2Loading() {
  return <LiquidGlassLoading />
}
